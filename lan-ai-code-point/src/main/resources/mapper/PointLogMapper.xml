<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanhai.lanaicodemother.mapper.PointLogMapper">

    <!-- 批量插入积分流水记录 -->
    <insert id="batchInsert">
        INSERT INTO point_log (
        user_id, business_type, business_id, point_type,
        point_change, before_points, after_points, remark, createTime
        ) VALUES
        <foreach collection="logs" item="log" separator=",">
            (
            #{log.userId},
            #{log.businessType},
            #{log.businessId},
            #{log.pointType},
            #{log.pointChange},
            #{log.beforePoints},
            #{log.afterPoints},
            #{log.remark},
            NOW()
            )
        </foreach>
    </insert>

</mapper>
